name: CI

on:
  pull_request:
  push:
    branches:
      - master
      - 'agent/**'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Validate RBAC policy
        run: npm run validate:rbac

      - name: Validate session policy
        run: npm run validate:session

      - name: Validate UI access policy
        run: npm run validate:ui-access

      - name: Validate smart inventory
        run: npm run validate:inventory

      - name: Validate customer privacy
        run: npm run validate:customer-privacy

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          VUE_APP_FIREBASE_API_KEY: ci-placeholder
          VUE_APP_FIREBASE_AUTH_DOMAIN: ci.example.invalid
          VUE_APP_FIREBASE_PROJECT_ID: ci-project
          VUE_APP_FIREBASE_MESSAGING_SENDER_ID: '000000000000'
          VUE_APP_FIREBASE_APP_ID: '1:000000000000:web:0000000000000000000000'
